{
  "name": "zelle-verification",
  "version": "1.0.0",
  "description": "Zelle verification form with Telegram integration",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  }
}
